import {useEffect} from 'react';import {Link} from 'react-router-dom';import {money} from '../data/products';
export default function ProductModal({product,onClose}){useEffect(()=>{if(!product)return;document.body.classList.add('modal-open');const f=e=>e.key==='Escape'&&onClose();document.addEventListener('keydown',f);return()=>{document.body.classList.remove('modal-open');document.removeEventListener('keydown',f)}},[product,onClose]);if(!product)return null;return <div className="panel-modal open"><div className="panel-modal-backdrop" onClick={onClose}/><div className="panel-modal-card" role="dialog" aria-modal="true" aria-labelledby="panelModalTitle"><button className="panel-modal-close" onClick={onClose} aria-label="Закрыть">×</button><div className="panel-modal-kicker">ВЫБРАННАЯ МОДЕЛЬ</div><h3 id="panelModalTitle">{product.name}</h3><p>{product.product==='acoustic'?'Акустическая панель':'Бамбуковая панель'}</p><div className="panel-modal-price">{money(product.price)} / м²</div><p className="panel-modal-copy">Введите размеры стен на следующем шаге — калькулятор посчитает необходимый объём и ориентировочную стоимость материала.</p><Link className="btn btn-primary" onClick={onClose} to={`/calculator?product=${product.product}&model=${encodeURIComponent(product.name)}&price=${product.price}`}>Рассчитать эту модель ↗</Link></div></div>}
